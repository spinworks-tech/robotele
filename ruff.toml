[lint]
# Pin the rule selection explicitly rather than relying on ruff's default,
# which has grown across versions (pyupgrade/isort/simplify/pylint rules
# were added to defaults after this was last checked against the codebase).
select = ["E4", "E7", "E9", "F"]
